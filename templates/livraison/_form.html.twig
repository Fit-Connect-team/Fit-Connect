{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="billing-address-form">
        <p>
            {{ form_widget(form.produit, {'attr': {'placeholder': 'Enter le id produit ..', 'class': 'form-control'}}) }}
        </p>
    </div>
    <br>
<button class="btn btn-primary">{{ button_label|default('Next') }}</button>
{{ form_end(form) }}
